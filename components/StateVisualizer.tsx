
import React from 'react';

// This component's functionality has been merged into OutputDisplay.tsx
// It is kept to avoid breaking imports but no longer renders anything.
export const StateVisualizer: React.FC = () => {
  return null;
};
